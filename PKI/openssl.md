# openssl 相關概念 && 指令

```bash
### 用來產生 token
$# openssl rand -hex 16


### 用來查看系統支援的 OpenSSL 加速引擎資訊
$# openssl engine -t
(rdrand) Intel RDRAND engine
     [ available ]
(dynamic) Dynamic engine loading support
     [ unavailable ]

### 
```
